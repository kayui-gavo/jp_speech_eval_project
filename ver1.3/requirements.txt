numpy>=1.24,<2.0
scipy>=1.10,<2.0
librosa>=0.10.1,<1.0
soundfile>=0.12,<1.0
matplotlib>=3.7,<4.0
# Drop-in OpenJTalk wrapper used by the selected Japanese phone-CTC model's
# training-label pipeline. The import name remains `pyopenjtalk`.
pyopenjtalk-plus==0.4.1.post8
pyworld-prebuilt>=0.3.5,<0.4
sounddevice>=0.4,<1.0
pandas>=2.0,<3.0
faster-whisper>=1.2,<2.0

# SSL/phone-CTC features. Keep stable major versions: Transformers 5 changed
# several model-loading APIs while the current research stack was built/tested
# against the Transformers 4 family.
torch>=2.0,<3.0
transformers>=4.30,<5.0

# Optional but recommended for higher-quality cached teacher references.
google-cloud-texttospeech>=2.16,<3.0

# Alternative ASR backend supported by src/jp_speech_eval/asr.py:
# openai-whisper
