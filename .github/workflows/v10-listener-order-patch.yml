name: v10-listener-order-patch
on:
  push:
    paths:
      - '.github/workflows/v10-listener-order-patch.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: free-speech-promotion-readiness-v10
          fetch-depth: 0
      - name: Apply deterministic blinded presentation ordering
        run: python ver1.3/scripts/_patch_listener_order_v10.py
      - name: Validate and commit
        run: |
          python -m py_compile ver1.3/scripts/free_speech_listener_server_v10.py
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add ver1.3/scripts/free_speech_listener_server_v10.py
          git commit -m 'research: randomize blinded listener order by rater'
          git push origin HEAD:free-speech-promotion-readiness-v10
